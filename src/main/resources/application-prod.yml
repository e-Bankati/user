server:
  port: 8010

eureka:
  instance:
    hostname: bankatii-user-server.onrender.com
  client:
    serviceUrl:
      defaultZone:
        ${EUREKA_HOST:https://bankatii-discovery.onrender.com/eureka/}
    register-with-eureka: true
    fetch-registry: true
  server:
    enable-self-preservation: false